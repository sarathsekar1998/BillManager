import React from "react";
import { Text, View } from "react-native";

const SignIn = () => {
    return (
        <View style={{ width: '100%', height: '100%', justifyContent: 'center', alignItems: 'center', backgroundColor: 'orange' }}>
            <Text>Sign In</Text>
        </View>
    );
};

export default SignIn;